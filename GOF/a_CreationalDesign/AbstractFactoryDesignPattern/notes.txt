Abstract Factory Pattern →
🔹 Creates related objects using interfaces, not exact classes.
🔹 Ensures consistency in object families.

✔️ Key idea: Client code relies on interfaces, making it independent of specific implementations.