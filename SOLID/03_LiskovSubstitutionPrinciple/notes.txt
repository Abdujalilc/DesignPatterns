	!!! If a child class replaces its parent class, everything should still work correctly.


	!!! Don't force things to fit where they don't belong. 
		If an object replaces another, it should work naturally.